fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9
pydantic==2.9.2
pydantic-settings==2.5.2
openai==1.51.0
faiss-cpu==1.8.0
pdfplumber==0.11.4
numpy==1.26.4
python-dotenv==1.0.1
tenacity==8.5.0
slowapi==0.1.9
httpx==0.27.2

# fastembed uses ONNX Runtime (NOT PyTorch) — ~50 MB install, ~80 MB RAM.
# This replaces sentence-transformers which dragged in torch (~500 MB RAM).
fastembed==0.4.2

# Hybrid retrieval (BM25 sparse — cross-encoder reranker disabled by default)
rank-bm25==0.2.2

# Learner memory persistence
aiosqlite==0.20.0
sqlalchemy[asyncio]==2.0.36
alembic==1.13.3
